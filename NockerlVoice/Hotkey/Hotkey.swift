import Foundation

/// Hotkey module constants. The global trigger is a double-tap of the **Right**
/// Command key, distinguished from Left Command by hardware key code.
enum HotkeyKeycode {
    /// Right Command. (Left Command is 55.)
    static let rightCommand: Int64 = 54
}
