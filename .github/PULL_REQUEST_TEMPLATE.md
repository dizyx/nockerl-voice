**What changed**

**Why**

**How you tested it**

Note the macOS version you tested on. Screen recordings help for UI changes.

**Checklist**

- [ ] `xcodebuild test -scheme NockerlVoice -destination 'platform=macOS'` passes
- [ ] `project.yml` was edited rather than the generated `.xcodeproj`
